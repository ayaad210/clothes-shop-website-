
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - My ASP.NET Application</title>
    <link href="~/Content/bootstrap.css" rel="stylesheet" />
    <link href="~/Content/bootstrap-theme.css" rel="stylesheet" />
    <link href="~/Content/ErrorStyle.css" rel="stylesheet" />

    <style>
        /*.navbar-right {
            float: right !important;
            margin-right: 20px;
            margin-left: 20px;
        }*/
    </style>
</head>
<body>
    <nav class="navbar navbar-inverse navbar-fixed-top role=" navigation" ">
        <div class="container">
            @Html.Partial("BasicNavbar", new Shop.Models.BasicNavBarModel() { user = new ShopDBEntities1().Users.Where(u => u.id == 2).First(), PageName = "Sells" })

            <div class="navbar-right"> @Html.Action("Summery", "Cart")</div>
        </div>
    </nav>
    <div class="panel " style="height:60px"></div>









    <div class="visible-xs">
        @Html.Action("FlixableMenu", "Nav", new { CatigoryId = (ViewBag.SelectedCat), MobileLayout = true })
    </div>


    <div class="row panel">
        <div id="specialization" class="col-xs-3 hidden-xs">
            @Html.Action("FlixableMenu", "Nav", new { CatigoryId = (ViewBag.SelectedCat) })
        </div>
        <div class="col-xs-12 col-sm-8  ">
            @RenderBody()
        </div>
    </div>

</body>
</html>